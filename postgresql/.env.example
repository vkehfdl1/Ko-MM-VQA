# Copy this file to .env and adjust values as needed

# Database connection
POSTGRES_DB=autorag
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

# Host port mapping (host:container)
# Set to 5432 to match VectorChord docs, or change if occupied
PG_PORT=5432
